{
  "name": "linkmetur-landing",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["landing page"],
  "scripts": {
    "dev": "cd \"landing page\" && npm run dev",
    "build": "cd \"landing page\" && npm run build",
    "start": "cd \"landing page\" && npm run start",
    "lint": "cd \"landing page\" && npm run lint",
    "install:all": "npm install && cd \"landing page\" && npm install"
  }
}
